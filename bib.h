#ifndef bib_H // предотращение повтрного включения файла
#define bib_H 
#include <iostream>

extern float add(std::string firstarg, std::string secondarg);
extern float sub(std::string firstarg, std::string secondarg);
extern int multi(std::string firstarg, std::string secondarg);

#endif // также предотвращает повторное включение (конец включения если не определена)
 
